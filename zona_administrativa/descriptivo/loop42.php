<script>

var DBsource = document.getElementById('DBsource')
var targetDB0A = document.getElementById('DB0')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB0AOption = Array.from(targetDB0A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB0AOption) {
    targetDB0AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB1A = document.getElementById('DB1')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB1AOption = Array.from(targetDB1A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB1AOption) {
    targetDB1AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB2A = document.getElementById('DB2')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB2AOption = Array.from(targetDB2A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB2AOption) {
    targetDB2AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB3A = document.getElementById('DB3')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB3AOption = Array.from(targetDB3A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB3AOption) {
    targetDB3AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB4A = document.getElementById('DB4')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB4AOption = Array.from(targetDB4A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB4AOption) {
    targetDB4AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB5A = document.getElementById('DB5')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB5AOption = Array.from(targetDB5A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB5AOption) {
    targetDB5AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB6A = document.getElementById('DB6')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB6AOption = Array.from(targetDB6A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB6AOption) {
    targetDB6AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB7A = document.getElementById('DB7')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB7AOption = Array.from(targetDB7A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB7AOption) {
    targetDB7AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB8A = document.getElementById('DB8')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB8AOption = Array.from(targetDB8A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB8AOption) {
    targetDB8AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB9A = document.getElementById('DB9')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB9AOption = Array.from(targetDB9A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB9AOption) {
    targetDB9AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB10A = document.getElementById('DB10')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB10AOption = Array.from(targetDB10A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB10AOption) {
    targetDB10AOption.selected = true
  }

})








var DBsource = document.getElementById('DBsource')
var targetDB11A = document.getElementById('DB11')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB11AOption = Array.from(targetDB11A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB11AOption) {
    targetDB11AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB12A = document.getElementById('DB12')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB12AOption = Array.from(targetDB12A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB12AOption) {
    targetDB12AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB13A = document.getElementById('DB13')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB13AOption = Array.from(targetDB13A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB13AOption) {
    targetDB13AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB14A = document.getElementById('DB14')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB14AOption = Array.from(targetDB14A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB14AOption) {
    targetDB14AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB15A = document.getElementById('DB15')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB15AOption = Array.from(targetDB15A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB15AOption) {
    targetDB15AOption.selected = true
  }

})






var DBsource = document.getElementById('DBsource')
var targetDB16A = document.getElementById('DB16')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB16AOption = Array.from(targetDB16A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB16AOption) {
    targetDB16AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB17A = document.getElementById('DB17')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB17AOption = Array.from(targetDB17A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB17AOption) {
    targetDB17AOption.selected = true
  }

})






var DBsource = document.getElementById('DBsource')
var targetDB18A = document.getElementById('DB18')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB18AOption = Array.from(targetDB18A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB18AOption) {
    targetDB18AOption.selected = true
  }

})






var DBsource = document.getElementById('DBsource')
var targetDB19A = document.getElementById('DB19')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB19AOption = Array.from(targetDB19A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB19AOption) {
    targetDB19AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB20A = document.getElementById('DB20')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB20AOption = Array.from(targetDB20A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB20AOption) {
    targetDB20AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB21A = document.getElementById('DB21')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB21AOption = Array.from(targetDB21A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB21AOption) {
    targetDB21AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB22A = document.getElementById('DB22')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB22AOption = Array.from(targetDB22A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB22AOption) {
    targetDB22AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB23A = document.getElementById('DB23')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB23AOption = Array.from(targetDB23A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB23AOption) {
    targetDB23AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB24A = document.getElementById('DB24')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB24AOption = Array.from(targetDB24A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB24AOption) {
    targetDB24AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB25A = document.getElementById('DB25')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB25AOption = Array.from(targetDB25A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB25AOption) {
    targetDB25AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB26A = document.getElementById('DB26')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB26AOption = Array.from(targetDB26A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB26AOption) {
    targetDB26AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB27A = document.getElementById('DB27')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB27AOption = Array.from(targetDB27A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB27AOption) {
    targetDB27AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB28A = document.getElementById('DB28')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB28AOption = Array.from(targetDB28A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB28AOption) {
    targetDB28AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB29A = document.getElementById('DB29')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB29AOption = Array.from(targetDB29A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB29AOption) {
    targetDB29AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB30A = document.getElementById('DB30')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB30AOption = Array.from(targetDB30A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB30AOption) {
    targetDB30AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB31A = document.getElementById('DB31')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB31AOption = Array.from(targetDB31A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB31AOption) {
    targetDB31AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB32A = document.getElementById('DB32')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB32AOption = Array.from(targetDB32A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB32AOption) {
    targetDB32AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB33A = document.getElementById('DB33')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB33AOption = Array.from(targetDB33A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB33AOption) {
    targetDB33AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB34A = document.getElementById('DB34')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB34AOption = Array.from(targetDB34A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB34AOption) {
    targetDB34AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB35A = document.getElementById('DB35')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB35AOption = Array.from(targetDB35A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB35AOption) {
    targetDB35AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB36A = document.getElementById('DB36')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB36AOption = Array.from(targetDB36A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB36AOption) {
    targetDB36AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB37A = document.getElementById('DB37')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB37AOption = Array.from(targetDB37A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB37AOption) {
    targetDB37AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB38A = document.getElementById('DB38')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB38AOption = Array.from(targetDB38A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB38AOption) {
    targetDB38AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB39A = document.getElementById('DB39')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB39AOption = Array.from(targetDB39A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB39AOption) {
    targetDB39AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB40A = document.getElementById('DB40')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB40AOption = Array.from(targetDB40A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB40AOption) {
    targetDB40AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB41A = document.getElementById('DB41')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB41AOption = Array.from(targetDB41A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB41AOption) {
    targetDB41AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB42A = document.getElementById('DB42')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB42AOption = Array.from(targetDB42A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB42AOption) {
    targetDB42AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB43A = document.getElementById('DB43')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB43AOption = Array.from(targetDB43A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB43AOption) {
    targetDB43AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB44A = document.getElementById('DB44')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB44AOption = Array.from(targetDB44A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB44AOption) {
    targetDB44AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB45A = document.getElementById('DB45')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB45AOption = Array.from(targetDB45A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB45AOption) {
    targetDB45AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB46A = document.getElementById('DB46')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB46AOption = Array.from(targetDB46A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB46AOption) {
    targetDB46AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB47A = document.getElementById('DB47')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB47AOption = Array.from(targetDB47A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB47AOption) {
    targetDB47AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB48A = document.getElementById('DB48')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB48AOption = Array.from(targetDB48A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB48AOption) {
    targetDB48AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB49A = document.getElementById('DB49')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB49AOption = Array.from(targetDB49A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB49AOption) {
    targetDB49AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB50A = document.getElementById('DB50')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB50AOption = Array.from(targetDB50A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB50AOption) {
    targetDB50AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB51A = document.getElementById('DB51')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB51AOption = Array.from(targetDB51A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB51AOption) {
    targetDB51AOption.selected = true
  }

})



var DBsource = document.getElementById('DBsource')
var targetDB52A = document.getElementById('DB52')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB52AOption = Array.from(targetDB52A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB52AOption) {
    targetDB52AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB53A = document.getElementById('DB53')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB53AOption = Array.from(targetDB53A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB53AOption) {
    targetDB53AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB54A = document.getElementById('DB54')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB54AOption = Array.from(targetDB54A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB54AOption) {
    targetDB54AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB55A = document.getElementById('DB55')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB55AOption = Array.from(targetDB55A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB55AOption) {
    targetDB55AOption.selected = true
  }

})





var DBsource = document.getElementById('DBsource')
var targetDB56A = document.getElementById('DB56')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB56AOption = Array.from(targetDB56A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB56AOption) {
    targetDB56AOption.selected = true
  }

})

var DBsource = document.getElementById('DBsource')
var targetDB57A = document.getElementById('DB57')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB57AOption = Array.from(targetDB57A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB57AOption) {
    targetDB57AOption.selected = true
  }

})


var DBsource = document.getElementById('DBsource')
var targetDB58A = document.getElementById('DB58')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB58AOption = Array.from(targetDB58A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB58AOption) {
    targetDB58AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB59A = document.getElementById('DB59')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB59AOption = Array.from(targetDB59A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB59AOption) {
    targetDB59AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB60A = document.getElementById('DB60')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB60AOption = Array.from(targetDB60A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB60AOption) {
    targetDB60AOption.selected = true
  }

})




var DBsource = document.getElementById('DBsource')
var targetDB61A = document.getElementById('DB61')

DBsource.addEventListener('change', function () {
  var selectedOptionText = DBsource.selectedOptions[0].textContent.trim()

  var targetDB61AOption = Array.from(targetDB61A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetDB61AOption) {
    targetDB61AOption.selected = true
  }

})


















































































</script>