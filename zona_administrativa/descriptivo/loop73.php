<script>

var GCsource = document.getElementById('GCsource')
var targetGC0A = document.getElementById('GC0')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC0AOption = Array.from(targetGC0A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC0AOption) {
    targetGC0AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC1A = document.getElementById('GC1')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC1AOption = Array.from(targetGC1A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC1AOption) {
    targetGC1AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC2A = document.getElementById('GC2')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC2AOption = Array.from(targetGC2A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC2AOption) {
    targetGC2AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC3A = document.getElementById('GC3')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC3AOption = Array.from(targetGC3A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC3AOption) {
    targetGC3AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC4A = document.getElementById('GC4')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC4AOption = Array.from(targetGC4A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC4AOption) {
    targetGC4AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC5A = document.getElementById('GC5')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC5AOption = Array.from(targetGC5A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC5AOption) {
    targetGC5AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC6A = document.getElementById('GC6')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC6AOption = Array.from(targetGC6A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC6AOption) {
    targetGC6AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC7A = document.getElementById('GC7')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC7AOption = Array.from(targetGC7A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC7AOption) {
    targetGC7AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC8A = document.getElementById('GC8')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC8AOption = Array.from(targetGC8A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC8AOption) {
    targetGC8AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC9A = document.getElementById('GC9')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC9AOption = Array.from(targetGC9A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC9AOption) {
    targetGC9AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC10A = document.getElementById('GC10')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC10AOption = Array.from(targetGC10A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC10AOption) {
    targetGC10AOption.selected = true
  }

})








var GCsource = document.getElementById('GCsource')
var targetGC11A = document.getElementById('GC11')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC11AOption = Array.from(targetGC11A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC11AOption) {
    targetGC11AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC12A = document.getElementById('GC12')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC12AOption = Array.from(targetGC12A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC12AOption) {
    targetGC12AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC13A = document.getElementById('GC13')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC13AOption = Array.from(targetGC13A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC13AOption) {
    targetGC13AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC14A = document.getElementById('GC14')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC14AOption = Array.from(targetGC14A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC14AOption) {
    targetGC14AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC15A = document.getElementById('GC15')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC15AOption = Array.from(targetGC15A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC15AOption) {
    targetGC15AOption.selected = true
  }

})






var GCsource = document.getElementById('GCsource')
var targetGC16A = document.getElementById('GC16')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC16AOption = Array.from(targetGC16A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC16AOption) {
    targetGC16AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC17A = document.getElementById('GC17')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC17AOption = Array.from(targetGC17A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC17AOption) {
    targetGC17AOption.selected = true
  }

})






var GCsource = document.getElementById('GCsource')
var targetGC18A = document.getElementById('GC18')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC18AOption = Array.from(targetGC18A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC18AOption) {
    targetGC18AOption.selected = true
  }

})






var GCsource = document.getElementById('GCsource')
var targetGC19A = document.getElementById('GC19')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC19AOption = Array.from(targetGC19A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC19AOption) {
    targetGC19AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC20A = document.getElementById('GC20')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC20AOption = Array.from(targetGC20A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC20AOption) {
    targetGC20AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC21A = document.getElementById('GC21')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC21AOption = Array.from(targetGC21A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC21AOption) {
    targetGC21AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC22A = document.getElementById('GC22')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC22AOption = Array.from(targetGC22A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC22AOption) {
    targetGC22AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC23A = document.getElementById('GC23')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC23AOption = Array.from(targetGC23A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC23AOption) {
    targetGC23AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC24A = document.getElementById('GC24')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC24AOption = Array.from(targetGC24A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC24AOption) {
    targetGC24AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC25A = document.getElementById('GC25')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC25AOption = Array.from(targetGC25A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC25AOption) {
    targetGC25AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC26A = document.getElementById('GC26')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC26AOption = Array.from(targetGC26A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC26AOption) {
    targetGC26AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC27A = document.getElementById('GC27')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC27AOption = Array.from(targetGC27A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC27AOption) {
    targetGC27AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC28A = document.getElementById('GC28')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC28AOption = Array.from(targetGC28A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC28AOption) {
    targetGC28AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC29A = document.getElementById('GC29')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC29AOption = Array.from(targetGC29A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC29AOption) {
    targetGC29AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC30A = document.getElementById('GC30')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC30AOption = Array.from(targetGC30A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC30AOption) {
    targetGC30AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC31A = document.getElementById('GC31')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC31AOption = Array.from(targetGC31A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC31AOption) {
    targetGC31AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC32A = document.getElementById('GC32')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC32AOption = Array.from(targetGC32A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC32AOption) {
    targetGC32AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC33A = document.getElementById('GC33')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC33AOption = Array.from(targetGC33A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC33AOption) {
    targetGC33AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC34A = document.getElementById('GC34')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC34AOption = Array.from(targetGC34A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC34AOption) {
    targetGC34AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC35A = document.getElementById('GC35')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC35AOption = Array.from(targetGC35A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC35AOption) {
    targetGC35AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC36A = document.getElementById('GC36')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC36AOption = Array.from(targetGC36A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC36AOption) {
    targetGC36AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC37A = document.getElementById('GC37')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC37AOption = Array.from(targetGC37A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC37AOption) {
    targetGC37AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC38A = document.getElementById('GC38')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC38AOption = Array.from(targetGC38A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC38AOption) {
    targetGC38AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC39A = document.getElementById('GC39')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC39AOption = Array.from(targetGC39A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC39AOption) {
    targetGC39AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC40A = document.getElementById('GC40')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC40AOption = Array.from(targetGC40A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC40AOption) {
    targetGC40AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC41A = document.getElementById('GC41')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC41AOption = Array.from(targetGC41A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC41AOption) {
    targetGC41AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC42A = document.getElementById('GC42')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC42AOption = Array.from(targetGC42A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC42AOption) {
    targetGC42AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC43A = document.getElementById('GC43')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC43AOption = Array.from(targetGC43A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC43AOption) {
    targetGC43AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC44A = document.getElementById('GC44')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC44AOption = Array.from(targetGC44A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC44AOption) {
    targetGC44AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC45A = document.getElementById('GC45')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC45AOption = Array.from(targetGC45A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC45AOption) {
    targetGC45AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC46A = document.getElementById('GC46')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC46AOption = Array.from(targetGC46A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC46AOption) {
    targetGC46AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC47A = document.getElementById('GC47')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC47AOption = Array.from(targetGC47A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC47AOption) {
    targetGC47AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC48A = document.getElementById('GC48')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC48AOption = Array.from(targetGC48A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC48AOption) {
    targetGC48AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC49A = document.getElementById('GC49')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC49AOption = Array.from(targetGC49A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC49AOption) {
    targetGC49AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC50A = document.getElementById('GC50')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC50AOption = Array.from(targetGC50A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC50AOption) {
    targetGC50AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC51A = document.getElementById('GC51')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC51AOption = Array.from(targetGC51A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC51AOption) {
    targetGC51AOption.selected = true
  }

})



var GCsource = document.getElementById('GCsource')
var targetGC52A = document.getElementById('GC52')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC52AOption = Array.from(targetGC52A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC52AOption) {
    targetGC52AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC53A = document.getElementById('GC53')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC53AOption = Array.from(targetGC53A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC53AOption) {
    targetGC53AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC54A = document.getElementById('GC54')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC54AOption = Array.from(targetGC54A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC54AOption) {
    targetGC54AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC55A = document.getElementById('GC55')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC55AOption = Array.from(targetGC55A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC55AOption) {
    targetGC55AOption.selected = true
  }

})





var GCsource = document.getElementById('GCsource')
var targetGC56A = document.getElementById('GC56')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC56AOption = Array.from(targetGC56A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC56AOption) {
    targetGC56AOption.selected = true
  }

})

var GCsource = document.getElementById('GCsource')
var targetGC57A = document.getElementById('GC57')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC57AOption = Array.from(targetGC57A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC57AOption) {
    targetGC57AOption.selected = true
  }

})


var GCsource = document.getElementById('GCsource')
var targetGC58A = document.getElementById('GC58')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC58AOption = Array.from(targetGC58A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC58AOption) {
    targetGC58AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC59A = document.getElementById('GC59')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC59AOption = Array.from(targetGC59A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC59AOption) {
    targetGC59AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC60A = document.getElementById('GC60')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC60AOption = Array.from(targetGC60A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC60AOption) {
    targetGC60AOption.selected = true
  }

})




var GCsource = document.getElementById('GCsource')
var targetGC61A = document.getElementById('GC61')

GCsource.addEventListener('change', function () {
  var selectedOptionText = GCsource.selectedOptions[0].textContent.trim()

  var targetGC61AOption = Array.from(targetGC61A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetGC61AOption) {
    targetGC61AOption.selected = true
  }

})


















































































</script>