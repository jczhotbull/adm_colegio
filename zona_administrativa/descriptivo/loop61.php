<script>

var FAsource = document.getElementById('FAsource')
var targetFA0A = document.getElementById('FA0')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA0AOption = Array.from(targetFA0A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA0AOption) {
    targetFA0AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA1A = document.getElementById('FA1')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA1AOption = Array.from(targetFA1A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA1AOption) {
    targetFA1AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA2A = document.getElementById('FA2')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA2AOption = Array.from(targetFA2A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA2AOption) {
    targetFA2AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA3A = document.getElementById('FA3')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA3AOption = Array.from(targetFA3A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA3AOption) {
    targetFA3AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA4A = document.getElementById('FA4')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA4AOption = Array.from(targetFA4A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA4AOption) {
    targetFA4AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA5A = document.getElementById('FA5')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA5AOption = Array.from(targetFA5A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA5AOption) {
    targetFA5AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA6A = document.getElementById('FA6')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA6AOption = Array.from(targetFA6A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA6AOption) {
    targetFA6AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA7A = document.getElementById('FA7')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA7AOption = Array.from(targetFA7A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA7AOption) {
    targetFA7AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA8A = document.getElementById('FA8')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA8AOption = Array.from(targetFA8A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA8AOption) {
    targetFA8AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA9A = document.getElementById('FA9')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA9AOption = Array.from(targetFA9A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA9AOption) {
    targetFA9AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA10A = document.getElementById('FA10')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA10AOption = Array.from(targetFA10A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA10AOption) {
    targetFA10AOption.selected = true
  }

})








var FAsource = document.getElementById('FAsource')
var targetFA11A = document.getElementById('FA11')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA11AOption = Array.from(targetFA11A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA11AOption) {
    targetFA11AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA12A = document.getElementById('FA12')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA12AOption = Array.from(targetFA12A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA12AOption) {
    targetFA12AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA13A = document.getElementById('FA13')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA13AOption = Array.from(targetFA13A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA13AOption) {
    targetFA13AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA14A = document.getElementById('FA14')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA14AOption = Array.from(targetFA14A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA14AOption) {
    targetFA14AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA15A = document.getElementById('FA15')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA15AOption = Array.from(targetFA15A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA15AOption) {
    targetFA15AOption.selected = true
  }

})






var FAsource = document.getElementById('FAsource')
var targetFA16A = document.getElementById('FA16')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA16AOption = Array.from(targetFA16A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA16AOption) {
    targetFA16AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA17A = document.getElementById('FA17')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA17AOption = Array.from(targetFA17A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA17AOption) {
    targetFA17AOption.selected = true
  }

})






var FAsource = document.getElementById('FAsource')
var targetFA18A = document.getElementById('FA18')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA18AOption = Array.from(targetFA18A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA18AOption) {
    targetFA18AOption.selected = true
  }

})






var FAsource = document.getElementById('FAsource')
var targetFA19A = document.getElementById('FA19')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA19AOption = Array.from(targetFA19A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA19AOption) {
    targetFA19AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA20A = document.getElementById('FA20')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA20AOption = Array.from(targetFA20A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA20AOption) {
    targetFA20AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA21A = document.getElementById('FA21')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA21AOption = Array.from(targetFA21A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA21AOption) {
    targetFA21AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA22A = document.getElementById('FA22')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA22AOption = Array.from(targetFA22A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA22AOption) {
    targetFA22AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA23A = document.getElementById('FA23')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA23AOption = Array.from(targetFA23A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA23AOption) {
    targetFA23AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA24A = document.getElementById('FA24')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA24AOption = Array.from(targetFA24A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA24AOption) {
    targetFA24AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA25A = document.getElementById('FA25')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA25AOption = Array.from(targetFA25A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA25AOption) {
    targetFA25AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA26A = document.getElementById('FA26')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA26AOption = Array.from(targetFA26A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA26AOption) {
    targetFA26AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA27A = document.getElementById('FA27')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA27AOption = Array.from(targetFA27A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA27AOption) {
    targetFA27AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA28A = document.getElementById('FA28')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA28AOption = Array.from(targetFA28A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA28AOption) {
    targetFA28AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA29A = document.getElementById('FA29')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA29AOption = Array.from(targetFA29A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA29AOption) {
    targetFA29AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA30A = document.getElementById('FA30')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA30AOption = Array.from(targetFA30A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA30AOption) {
    targetFA30AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA31A = document.getElementById('FA31')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA31AOption = Array.from(targetFA31A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA31AOption) {
    targetFA31AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA32A = document.getElementById('FA32')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA32AOption = Array.from(targetFA32A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA32AOption) {
    targetFA32AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA33A = document.getElementById('FA33')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA33AOption = Array.from(targetFA33A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA33AOption) {
    targetFA33AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA34A = document.getElementById('FA34')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA34AOption = Array.from(targetFA34A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA34AOption) {
    targetFA34AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA35A = document.getElementById('FA35')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA35AOption = Array.from(targetFA35A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA35AOption) {
    targetFA35AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA36A = document.getElementById('FA36')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA36AOption = Array.from(targetFA36A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA36AOption) {
    targetFA36AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA37A = document.getElementById('FA37')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA37AOption = Array.from(targetFA37A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA37AOption) {
    targetFA37AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA38A = document.getElementById('FA38')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA38AOption = Array.from(targetFA38A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA38AOption) {
    targetFA38AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA39A = document.getElementById('FA39')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA39AOption = Array.from(targetFA39A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA39AOption) {
    targetFA39AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA40A = document.getElementById('FA40')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA40AOption = Array.from(targetFA40A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA40AOption) {
    targetFA40AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA41A = document.getElementById('FA41')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA41AOption = Array.from(targetFA41A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA41AOption) {
    targetFA41AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA42A = document.getElementById('FA42')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA42AOption = Array.from(targetFA42A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA42AOption) {
    targetFA42AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA43A = document.getElementById('FA43')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA43AOption = Array.from(targetFA43A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA43AOption) {
    targetFA43AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA44A = document.getElementById('FA44')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA44AOption = Array.from(targetFA44A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA44AOption) {
    targetFA44AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA45A = document.getElementById('FA45')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA45AOption = Array.from(targetFA45A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA45AOption) {
    targetFA45AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA46A = document.getElementById('FA46')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA46AOption = Array.from(targetFA46A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA46AOption) {
    targetFA46AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA47A = document.getElementById('FA47')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA47AOption = Array.from(targetFA47A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA47AOption) {
    targetFA47AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA48A = document.getElementById('FA48')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA48AOption = Array.from(targetFA48A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA48AOption) {
    targetFA48AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA49A = document.getElementById('FA49')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA49AOption = Array.from(targetFA49A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA49AOption) {
    targetFA49AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA50A = document.getElementById('FA50')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA50AOption = Array.from(targetFA50A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA50AOption) {
    targetFA50AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA51A = document.getElementById('FA51')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA51AOption = Array.from(targetFA51A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA51AOption) {
    targetFA51AOption.selected = true
  }

})



var FAsource = document.getElementById('FAsource')
var targetFA52A = document.getElementById('FA52')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA52AOption = Array.from(targetFA52A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA52AOption) {
    targetFA52AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA53A = document.getElementById('FA53')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA53AOption = Array.from(targetFA53A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA53AOption) {
    targetFA53AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA54A = document.getElementById('FA54')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA54AOption = Array.from(targetFA54A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA54AOption) {
    targetFA54AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA55A = document.getElementById('FA55')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA55AOption = Array.from(targetFA55A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA55AOption) {
    targetFA55AOption.selected = true
  }

})





var FAsource = document.getElementById('FAsource')
var targetFA56A = document.getElementById('FA56')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA56AOption = Array.from(targetFA56A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA56AOption) {
    targetFA56AOption.selected = true
  }

})

var FAsource = document.getElementById('FAsource')
var targetFA57A = document.getElementById('FA57')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA57AOption = Array.from(targetFA57A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA57AOption) {
    targetFA57AOption.selected = true
  }

})


var FAsource = document.getElementById('FAsource')
var targetFA58A = document.getElementById('FA58')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA58AOption = Array.from(targetFA58A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA58AOption) {
    targetFA58AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA59A = document.getElementById('FA59')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA59AOption = Array.from(targetFA59A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA59AOption) {
    targetFA59AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA60A = document.getElementById('FA60')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA60AOption = Array.from(targetFA60A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA60AOption) {
    targetFA60AOption.selected = true
  }

})




var FAsource = document.getElementById('FAsource')
var targetFA61A = document.getElementById('FA61')

FAsource.addEventListener('change', function () {
  var selectedOptionText = FAsource.selectedOptions[0].textContent.trim()

  var targetFA61AOption = Array.from(targetFA61A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetFA61AOption) {
    targetFA61AOption.selected = true
  }

})


















































































</script>