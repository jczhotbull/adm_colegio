<script>

var ECsource = document.getElementById('ECsource')
var targetEC0A = document.getElementById('EC0')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC0AOption = Array.from(targetEC0A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC0AOption) {
    targetEC0AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC1A = document.getElementById('EC1')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC1AOption = Array.from(targetEC1A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC1AOption) {
    targetEC1AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC2A = document.getElementById('EC2')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC2AOption = Array.from(targetEC2A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC2AOption) {
    targetEC2AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC3A = document.getElementById('EC3')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC3AOption = Array.from(targetEC3A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC3AOption) {
    targetEC3AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC4A = document.getElementById('EC4')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC4AOption = Array.from(targetEC4A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC4AOption) {
    targetEC4AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC5A = document.getElementById('EC5')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC5AOption = Array.from(targetEC5A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC5AOption) {
    targetEC5AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC6A = document.getElementById('EC6')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC6AOption = Array.from(targetEC6A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC6AOption) {
    targetEC6AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC7A = document.getElementById('EC7')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC7AOption = Array.from(targetEC7A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC7AOption) {
    targetEC7AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC8A = document.getElementById('EC8')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC8AOption = Array.from(targetEC8A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC8AOption) {
    targetEC8AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC9A = document.getElementById('EC9')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC9AOption = Array.from(targetEC9A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC9AOption) {
    targetEC9AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC10A = document.getElementById('EC10')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC10AOption = Array.from(targetEC10A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC10AOption) {
    targetEC10AOption.selected = true
  }

})








var ECsource = document.getElementById('ECsource')
var targetEC11A = document.getElementById('EC11')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC11AOption = Array.from(targetEC11A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC11AOption) {
    targetEC11AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC12A = document.getElementById('EC12')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC12AOption = Array.from(targetEC12A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC12AOption) {
    targetEC12AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC13A = document.getElementById('EC13')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC13AOption = Array.from(targetEC13A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC13AOption) {
    targetEC13AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC14A = document.getElementById('EC14')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC14AOption = Array.from(targetEC14A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC14AOption) {
    targetEC14AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC15A = document.getElementById('EC15')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC15AOption = Array.from(targetEC15A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC15AOption) {
    targetEC15AOption.selected = true
  }

})






var ECsource = document.getElementById('ECsource')
var targetEC16A = document.getElementById('EC16')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC16AOption = Array.from(targetEC16A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC16AOption) {
    targetEC16AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC17A = document.getElementById('EC17')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC17AOption = Array.from(targetEC17A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC17AOption) {
    targetEC17AOption.selected = true
  }

})






var ECsource = document.getElementById('ECsource')
var targetEC18A = document.getElementById('EC18')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC18AOption = Array.from(targetEC18A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC18AOption) {
    targetEC18AOption.selected = true
  }

})






var ECsource = document.getElementById('ECsource')
var targetEC19A = document.getElementById('EC19')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC19AOption = Array.from(targetEC19A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC19AOption) {
    targetEC19AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC20A = document.getElementById('EC20')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC20AOption = Array.from(targetEC20A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC20AOption) {
    targetEC20AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC21A = document.getElementById('EC21')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC21AOption = Array.from(targetEC21A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC21AOption) {
    targetEC21AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC22A = document.getElementById('EC22')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC22AOption = Array.from(targetEC22A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC22AOption) {
    targetEC22AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC23A = document.getElementById('EC23')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC23AOption = Array.from(targetEC23A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC23AOption) {
    targetEC23AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC24A = document.getElementById('EC24')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC24AOption = Array.from(targetEC24A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC24AOption) {
    targetEC24AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC25A = document.getElementById('EC25')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC25AOption = Array.from(targetEC25A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC25AOption) {
    targetEC25AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC26A = document.getElementById('EC26')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC26AOption = Array.from(targetEC26A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC26AOption) {
    targetEC26AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC27A = document.getElementById('EC27')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC27AOption = Array.from(targetEC27A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC27AOption) {
    targetEC27AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC28A = document.getElementById('EC28')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC28AOption = Array.from(targetEC28A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC28AOption) {
    targetEC28AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC29A = document.getElementById('EC29')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC29AOption = Array.from(targetEC29A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC29AOption) {
    targetEC29AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC30A = document.getElementById('EC30')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC30AOption = Array.from(targetEC30A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC30AOption) {
    targetEC30AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC31A = document.getElementById('EC31')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC31AOption = Array.from(targetEC31A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC31AOption) {
    targetEC31AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC32A = document.getElementById('EC32')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC32AOption = Array.from(targetEC32A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC32AOption) {
    targetEC32AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC33A = document.getElementById('EC33')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC33AOption = Array.from(targetEC33A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC33AOption) {
    targetEC33AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC34A = document.getElementById('EC34')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC34AOption = Array.from(targetEC34A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC34AOption) {
    targetEC34AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC35A = document.getElementById('EC35')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC35AOption = Array.from(targetEC35A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC35AOption) {
    targetEC35AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC36A = document.getElementById('EC36')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC36AOption = Array.from(targetEC36A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC36AOption) {
    targetEC36AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC37A = document.getElementById('EC37')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC37AOption = Array.from(targetEC37A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC37AOption) {
    targetEC37AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC38A = document.getElementById('EC38')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC38AOption = Array.from(targetEC38A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC38AOption) {
    targetEC38AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC39A = document.getElementById('EC39')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC39AOption = Array.from(targetEC39A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC39AOption) {
    targetEC39AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC40A = document.getElementById('EC40')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC40AOption = Array.from(targetEC40A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC40AOption) {
    targetEC40AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC41A = document.getElementById('EC41')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC41AOption = Array.from(targetEC41A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC41AOption) {
    targetEC41AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC42A = document.getElementById('EC42')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC42AOption = Array.from(targetEC42A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC42AOption) {
    targetEC42AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC43A = document.getElementById('EC43')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC43AOption = Array.from(targetEC43A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC43AOption) {
    targetEC43AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC44A = document.getElementById('EC44')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC44AOption = Array.from(targetEC44A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC44AOption) {
    targetEC44AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC45A = document.getElementById('EC45')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC45AOption = Array.from(targetEC45A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC45AOption) {
    targetEC45AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC46A = document.getElementById('EC46')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC46AOption = Array.from(targetEC46A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC46AOption) {
    targetEC46AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC47A = document.getElementById('EC47')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC47AOption = Array.from(targetEC47A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC47AOption) {
    targetEC47AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC48A = document.getElementById('EC48')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC48AOption = Array.from(targetEC48A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC48AOption) {
    targetEC48AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC49A = document.getElementById('EC49')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC49AOption = Array.from(targetEC49A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC49AOption) {
    targetEC49AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC50A = document.getElementById('EC50')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC50AOption = Array.from(targetEC50A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC50AOption) {
    targetEC50AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC51A = document.getElementById('EC51')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC51AOption = Array.from(targetEC51A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC51AOption) {
    targetEC51AOption.selected = true
  }

})



var ECsource = document.getElementById('ECsource')
var targetEC52A = document.getElementById('EC52')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC52AOption = Array.from(targetEC52A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC52AOption) {
    targetEC52AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC53A = document.getElementById('EC53')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC53AOption = Array.from(targetEC53A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC53AOption) {
    targetEC53AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC54A = document.getElementById('EC54')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC54AOption = Array.from(targetEC54A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC54AOption) {
    targetEC54AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC55A = document.getElementById('EC55')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC55AOption = Array.from(targetEC55A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC55AOption) {
    targetEC55AOption.selected = true
  }

})





var ECsource = document.getElementById('ECsource')
var targetEC56A = document.getElementById('EC56')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC56AOption = Array.from(targetEC56A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC56AOption) {
    targetEC56AOption.selected = true
  }

})

var ECsource = document.getElementById('ECsource')
var targetEC57A = document.getElementById('EC57')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC57AOption = Array.from(targetEC57A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC57AOption) {
    targetEC57AOption.selected = true
  }

})


var ECsource = document.getElementById('ECsource')
var targetEC58A = document.getElementById('EC58')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC58AOption = Array.from(targetEC58A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC58AOption) {
    targetEC58AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC59A = document.getElementById('EC59')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC59AOption = Array.from(targetEC59A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC59AOption) {
    targetEC59AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC60A = document.getElementById('EC60')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC60AOption = Array.from(targetEC60A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC60AOption) {
    targetEC60AOption.selected = true
  }

})




var ECsource = document.getElementById('ECsource')
var targetEC61A = document.getElementById('EC61')

ECsource.addEventListener('change', function () {
  var selectedOptionText = ECsource.selectedOptions[0].textContent.trim()

  var targetEC61AOption = Array.from(targetEC61A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEC61AOption) {
    targetEC61AOption.selected = true
  }

})


















































































</script>