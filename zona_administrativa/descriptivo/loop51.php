<script>

var EAsource = document.getElementById('EAsource')
var targetEA0A = document.getElementById('EA0')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA0AOption = Array.from(targetEA0A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA0AOption) {
    targetEA0AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA1A = document.getElementById('EA1')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA1AOption = Array.from(targetEA1A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA1AOption) {
    targetEA1AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA2A = document.getElementById('EA2')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA2AOption = Array.from(targetEA2A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA2AOption) {
    targetEA2AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA3A = document.getElementById('EA3')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA3AOption = Array.from(targetEA3A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA3AOption) {
    targetEA3AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA4A = document.getElementById('EA4')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA4AOption = Array.from(targetEA4A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA4AOption) {
    targetEA4AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA5A = document.getElementById('EA5')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA5AOption = Array.from(targetEA5A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA5AOption) {
    targetEA5AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA6A = document.getElementById('EA6')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA6AOption = Array.from(targetEA6A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA6AOption) {
    targetEA6AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA7A = document.getElementById('EA7')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA7AOption = Array.from(targetEA7A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA7AOption) {
    targetEA7AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA8A = document.getElementById('EA8')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA8AOption = Array.from(targetEA8A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA8AOption) {
    targetEA8AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA9A = document.getElementById('EA9')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA9AOption = Array.from(targetEA9A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA9AOption) {
    targetEA9AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA10A = document.getElementById('EA10')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA10AOption = Array.from(targetEA10A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA10AOption) {
    targetEA10AOption.selected = true
  }

})








var EAsource = document.getElementById('EAsource')
var targetEA11A = document.getElementById('EA11')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA11AOption = Array.from(targetEA11A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA11AOption) {
    targetEA11AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA12A = document.getElementById('EA12')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA12AOption = Array.from(targetEA12A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA12AOption) {
    targetEA12AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA13A = document.getElementById('EA13')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA13AOption = Array.from(targetEA13A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA13AOption) {
    targetEA13AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA14A = document.getElementById('EA14')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA14AOption = Array.from(targetEA14A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA14AOption) {
    targetEA14AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA15A = document.getElementById('EA15')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA15AOption = Array.from(targetEA15A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA15AOption) {
    targetEA15AOption.selected = true
  }

})






var EAsource = document.getElementById('EAsource')
var targetEA16A = document.getElementById('EA16')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA16AOption = Array.from(targetEA16A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA16AOption) {
    targetEA16AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA17A = document.getElementById('EA17')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA17AOption = Array.from(targetEA17A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA17AOption) {
    targetEA17AOption.selected = true
  }

})






var EAsource = document.getElementById('EAsource')
var targetEA18A = document.getElementById('EA18')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA18AOption = Array.from(targetEA18A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA18AOption) {
    targetEA18AOption.selected = true
  }

})






var EAsource = document.getElementById('EAsource')
var targetEA19A = document.getElementById('EA19')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA19AOption = Array.from(targetEA19A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA19AOption) {
    targetEA19AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA20A = document.getElementById('EA20')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA20AOption = Array.from(targetEA20A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA20AOption) {
    targetEA20AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA21A = document.getElementById('EA21')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA21AOption = Array.from(targetEA21A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA21AOption) {
    targetEA21AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA22A = document.getElementById('EA22')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA22AOption = Array.from(targetEA22A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA22AOption) {
    targetEA22AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA23A = document.getElementById('EA23')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA23AOption = Array.from(targetEA23A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA23AOption) {
    targetEA23AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA24A = document.getElementById('EA24')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA24AOption = Array.from(targetEA24A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA24AOption) {
    targetEA24AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA25A = document.getElementById('EA25')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA25AOption = Array.from(targetEA25A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA25AOption) {
    targetEA25AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA26A = document.getElementById('EA26')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA26AOption = Array.from(targetEA26A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA26AOption) {
    targetEA26AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA27A = document.getElementById('EA27')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA27AOption = Array.from(targetEA27A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA27AOption) {
    targetEA27AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA28A = document.getElementById('EA28')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA28AOption = Array.from(targetEA28A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA28AOption) {
    targetEA28AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA29A = document.getElementById('EA29')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA29AOption = Array.from(targetEA29A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA29AOption) {
    targetEA29AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA30A = document.getElementById('EA30')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA30AOption = Array.from(targetEA30A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA30AOption) {
    targetEA30AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA31A = document.getElementById('EA31')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA31AOption = Array.from(targetEA31A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA31AOption) {
    targetEA31AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA32A = document.getElementById('EA32')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA32AOption = Array.from(targetEA32A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA32AOption) {
    targetEA32AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA33A = document.getElementById('EA33')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA33AOption = Array.from(targetEA33A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA33AOption) {
    targetEA33AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA34A = document.getElementById('EA34')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA34AOption = Array.from(targetEA34A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA34AOption) {
    targetEA34AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA35A = document.getElementById('EA35')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA35AOption = Array.from(targetEA35A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA35AOption) {
    targetEA35AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA36A = document.getElementById('EA36')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA36AOption = Array.from(targetEA36A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA36AOption) {
    targetEA36AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA37A = document.getElementById('EA37')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA37AOption = Array.from(targetEA37A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA37AOption) {
    targetEA37AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA38A = document.getElementById('EA38')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA38AOption = Array.from(targetEA38A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA38AOption) {
    targetEA38AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA39A = document.getElementById('EA39')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA39AOption = Array.from(targetEA39A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA39AOption) {
    targetEA39AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA40A = document.getElementById('EA40')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA40AOption = Array.from(targetEA40A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA40AOption) {
    targetEA40AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA41A = document.getElementById('EA41')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA41AOption = Array.from(targetEA41A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA41AOption) {
    targetEA41AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA42A = document.getElementById('EA42')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA42AOption = Array.from(targetEA42A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA42AOption) {
    targetEA42AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA43A = document.getElementById('EA43')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA43AOption = Array.from(targetEA43A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA43AOption) {
    targetEA43AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA44A = document.getElementById('EA44')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA44AOption = Array.from(targetEA44A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA44AOption) {
    targetEA44AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA45A = document.getElementById('EA45')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA45AOption = Array.from(targetEA45A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA45AOption) {
    targetEA45AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA46A = document.getElementById('EA46')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA46AOption = Array.from(targetEA46A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA46AOption) {
    targetEA46AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA47A = document.getElementById('EA47')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA47AOption = Array.from(targetEA47A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA47AOption) {
    targetEA47AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA48A = document.getElementById('EA48')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA48AOption = Array.from(targetEA48A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA48AOption) {
    targetEA48AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA49A = document.getElementById('EA49')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA49AOption = Array.from(targetEA49A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA49AOption) {
    targetEA49AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA50A = document.getElementById('EA50')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA50AOption = Array.from(targetEA50A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA50AOption) {
    targetEA50AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA51A = document.getElementById('EA51')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA51AOption = Array.from(targetEA51A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA51AOption) {
    targetEA51AOption.selected = true
  }

})



var EAsource = document.getElementById('EAsource')
var targetEA52A = document.getElementById('EA52')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA52AOption = Array.from(targetEA52A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA52AOption) {
    targetEA52AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA53A = document.getElementById('EA53')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA53AOption = Array.from(targetEA53A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA53AOption) {
    targetEA53AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA54A = document.getElementById('EA54')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA54AOption = Array.from(targetEA54A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA54AOption) {
    targetEA54AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA55A = document.getElementById('EA55')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA55AOption = Array.from(targetEA55A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA55AOption) {
    targetEA55AOption.selected = true
  }

})





var EAsource = document.getElementById('EAsource')
var targetEA56A = document.getElementById('EA56')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA56AOption = Array.from(targetEA56A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA56AOption) {
    targetEA56AOption.selected = true
  }

})

var EAsource = document.getElementById('EAsource')
var targetEA57A = document.getElementById('EA57')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA57AOption = Array.from(targetEA57A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA57AOption) {
    targetEA57AOption.selected = true
  }

})


var EAsource = document.getElementById('EAsource')
var targetEA58A = document.getElementById('EA58')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA58AOption = Array.from(targetEA58A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA58AOption) {
    targetEA58AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA59A = document.getElementById('EA59')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA59AOption = Array.from(targetEA59A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA59AOption) {
    targetEA59AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA60A = document.getElementById('EA60')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA60AOption = Array.from(targetEA60A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA60AOption) {
    targetEA60AOption.selected = true
  }

})




var EAsource = document.getElementById('EAsource')
var targetEA61A = document.getElementById('EA61')

EAsource.addEventListener('change', function () {
  var selectedOptionText = EAsource.selectedOptions[0].textContent.trim()

  var targetEA61AOption = Array.from(targetEA61A.options).find(function (option) {
    return option.textContent.trim() === selectedOptionText   
  })

  if (targetEA61AOption) {
    targetEA61AOption.selected = true
  }

})


















































































</script>